var app = angular.module('ticketToRide');

app.factory('Game', function (PlayerDataMin, Player, GameBoard) {

    //private data
    var opponents = [];
    var player;
    var board;
    var gameId = -1;
    var gameName = "";
    var gameHistory = [];
    var gameChat = [];

    //constructor 
    function Game (gameData) {

    }

    //public data

    return Game;
});