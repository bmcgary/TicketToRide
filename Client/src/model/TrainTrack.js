var app = angular.module('ticketToRide');

app.factory('TrainTrack', function (TrainCardColor) {

    //private data
    var trackId = -1;
    var trainsRequired = -1;
    var color = "";
    var coordinates = {}; //We need some kind of rectangle to contain here

    //constructor 
    function TrainTrack (gameData) {

    }

    //public data

    return TrainTrack;
});